#ifndef DEF_H
#define DEF_H
#include <stdio.h>
#include <string.h>
#include <unistd.h>

#define pf(text) printf(text)
#define te true
#define fe false
#define dk break
#define rn(n) return n
#define we(condition){} while(condition){}
#define sp(time) sleep(time)

#endif